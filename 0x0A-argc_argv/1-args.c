#include <stdio.h>
#include "main.h"

/*
 * main-  program prints a number, followed by a new line
 *@argc: count number of arguments
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
